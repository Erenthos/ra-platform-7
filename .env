# ─────────────────────────────────────────────
# 🌈 English Reverse Auction Platform - Environment Variables
# ─────────────────────────────────────────────

# PostgreSQL / Neon Database connection string
# (Make sure to include ?sslmode=require at the end for Render)
DATABASE_URL="postgresql://neondb_owner:npg_4i1IPgEpDtwO@ep-divine-cherry-a1jucg7b-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# NextAuth secret key (generate using: openssl rand -base64 32)
NEXTAUTH_SECRET="your-random-generated-secret"

# Base URL of your application
# Local: http://localhost:3000
# Render: https://ra-platform-7.onrender.com
NEXTAUTH_URL="http://localhost:3000"

# Optional: Node environment (Render automatically sets NODE_ENV=production)
NODE_ENV="development"

# ─────────────────────────────────────────────
# 🔧 Prisma
# ─────────────────────────────────────────────
# This is automatically used by Prisma CLI
# npx prisma migrate dev / deploy will use DATABASE_URL

# ─────────────────────────────────────────────
# 💌 Email (Optional Future Setup)
# ─────────────────────────────────────────────
# Add these only if you plan to enable email notifications later
# SMTP_HOST=smtp.yourmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=yourpassword

# ─────────────────────────────────────────────
# ✅ Notes
# ─────────────────────────────────────────────
# 1. Copy this file as `.env` in your project root.
# 2. For Render deployment:
#    - Add DATABASE_URL, NEXTAUTH_SECRET, and NEXTAUTH_URL
#      in Render → Environment → Add Environment Variables.
# 3. Make sure NEXTAUTH_URL matches exactly your Render app URL
#    (for example: https://ra-platform-7.onrender.com)

